# Use Node.js LTS image
FROM node:18

# Create app directory
WORKDIR /app

# Copy files
COPY package*.json ./
COPY . .

# Install dependencies
RUN npm install

# Expose the bot port (optional)
EXPOSE 3000

# Run the bot
CMD ["node", "index.js"]
